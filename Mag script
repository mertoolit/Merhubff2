-- Football Fusion 2 Mer Hub
-- Version: 1.0
-- Author: ChatGPT (for Mer Beauvoir)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- SETTINGS
local PAID_USER = false -- Set to true if paid user; for demo you can toggle this
local FREE_KEY = "MERHUBFREE" -- The key user must enter to unlock free version
local KEY_TRIES = 3

-- Color themes preset
local Themes = {
    ["Purple"] = Color3.fromRGB(128, 0, 255),
    ["Blue"] = Color3.fromRGB(0, 120, 255),
    ["Red"] = Color3.fromRGB(255, 50, 50),
    ["Green"] = Color3.fromRGB(0, 200, 0),
    ["Orange"] = Color3.fromRGB(255, 150, 0),
}

-- UTILITIES
local function Create(className, props)
    local obj = Instance.new(className)
    for k,v in pairs(props) do
        obj[k] = v
    end
    return obj
end

-- Clean up old GUI if exists
local oldGui = PlayerGui:FindFirstChild("MerHub")
if oldGui then oldGui:Destroy() end

-- MAIN GUI CONSTRUCTION
local MerHub = Create("ScreenGui", {Name = "MerHub", ResetOnSpawn = false, Parent = PlayerGui})

local MainFrame = Create("Frame", {
    Parent = MerHub,
    Size = UDim2.new(0, 420, 0, 300),
    Position = UDim2.new(0.5, -210, 0.5, -150),
    BackgroundColor3 = Color3.fromRGB(
